// export type Match = {id: number, expiryDate: Date}
// export type Winner = { match: Match, username: string, userId: number, maxScore: number }
// export type MatchScore = { matchId: number, matchExpiryDate: number, matchScore: number, score: number }

export type Product = {id: number, name: string, price: number, ordersCount: number}
export type Order = {id: number, userId: string, productId: string}
// export type Winner = { match: Match, username: string, userId: number, maxScore: number }
// export type MatchScore = { matchId: number, matchExpiryDate: number, matchScore: number, score: number }
